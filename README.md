# Numerical Analysis Suite
### A C-Powered Computational Engine with Interactive Python GUI

> **Version 1.0.0 — Pro Edition**  
> Lead Developer: **Somajit Deb** · Computer Science & Engineering · Khulna University

---

## Table of Contents

1. [Project Overview](#1-project-overview)
2. [Architecture Overview](#2-architecture-overview)
3. [Project Structure](#3-project-structure)
4. [System Requirements](#4-system-requirements)
5. [Installation & Build Guide](#5-installation--build-guide)
6. [License & Trial System](#6-license--trial-system)
7. [Running the Application](#7-running-the-application)
8. [User Interface Guide](#8-user-interface-guide)
   - [Dashboard (Home Tab)](#81-dashboard-home-tab)
   - [Root Finding Engine](#82-root-finding-engine)
   - [Linear Systems Engine](#83-linear-systems-engine)
9. [Supported Algorithms — Root Finding](#9-supported-algorithms--root-finding)
10. [Supported Algorithms — Linear Systems](#10-supported-algorithms--linear-systems)
11. [Equation Syntax Reference](#11-equation-syntax-reference)
12. [Keyboard Shortcuts & Controls](#12-keyboard-shortcuts--controls)
13. [Theming System](#13-theming-system)
14. [Source File Reference](#14-source-file-reference)
15. [Building for Distribution (PyInstaller)](#15-building-for-distribution-pyinstaller)
16. [Troubleshooting](#16-troubleshooting)

---

## 1. Project Overview

**Numerical Analysis Suite** is a desktop application that brings numerical computing to life through a visually rich, step-by-step animated interface. It is designed for students and engineers studying numerical methods, providing both a working computational tool and an educational visualizer.

The application is split into two independent computational engines:

| Module | Purpose |
|---|---|
| **Root Finding Engine** | Find roots of arbitrary single-variable equations using 8 classical algorithms |
| **Linear Systems Engine** | Solve N×N systems of linear equations step-by-step using 4 matrix methods |

**Key design philosophy:** The performance-critical math runs inside compiled C executables (`engine.exe`, `matrix_engine.exe`). Python handles only the GUI, subprocess communication, and rendering — giving the application native-speed numerics with a modern Python UI.

---

## 2. Architecture Overview

```
┌─────────────────────────────────────────────────────┐
│                   main.py (Python)                   │
│              Tkinter Application Host                │
│  ┌──────────────┐  ┌──────────────┐  ┌───────────┐  │
│  │  home_tab.py │  │ root_tab.py  │  │matrix_tab │  │
│  │  (Dashboard) │  │(Root Finder) │  │  .py      │  │
│  └──────────────┘  └──────┬───────┘  └─────┬─────┘  │
│                           │  subprocess     │        │
└───────────────────────────┼─────────────────┼────────┘
                            ▼                 ▼
              ┌─────────────────┐  ┌──────────────────┐
              │   engine.exe    │  │matrix_engine.exe  │
              │  (engine_main.c │  │ (matrix_main.c +  │
              │   + roots.c +   │  │   linalg.c)       │
              │   tinyexpr.c)   │  └──────────────────┘
              └─────────────────┘
```

Python calls the C executables via `subprocess`, passing all parameters as command-line arguments. The C engine runs the math, prints structured output lines to `stdout`, and Python parses them to drive the UI.

---

## 3. Project Structure

```
NUMERIC.../
│
├── build/                      # Compiled executables (generated by Makefile)
│   ├── engine.exe              # Root finding solver
│   └── matrix_engine.exe       # Linear systems solver
│
├── include/                    # (Alternative) C header location
│   ├── linalg.h
│   ├── roots.h
│   └── tinyexpr.h
│
├── src/                        # C source files
│   ├── engine_main.c           # Entry point for root finding
│   ├── roots.c                 # All root-finding algorithms
│   ├── linalg.c                # All linear algebra solvers
│   ├── matrix_main.c           # Entry point for matrix solver
│   └── tinyexpr.c              # Embedded math expression parser
│
├── config.py                   # Theme system, fonts, global helpers
├── home_tab.py                 # Dashboard / home screen UI
├── root_tab.py                 # Root Finding tab UI + solver interface
├── matrix_tab.py               # Linear Systems tab UI + solver interface
├── widgets.py                  # Reusable UI components (GlowButton, etc.)
├── main.py                     # Application entry point
├── license_manager.py          # Trial & license logic (Windows Registry)
├── license_gate.py             # License activation modal window
├── secret_key.py               # Stores SHA-256 hash of the valid license key
│
├── app_icon.ico                # Application window icon
├── main.spec                   # PyInstaller build spec
├── Makefile                    # Build script for C executables
└── LICENSE
```

---

## 4. System Requirements

| Requirement | Details |
|---|---|
| **Operating System** | Windows 10 / 11 (64-bit). License system uses Windows Registry (`winreg`). |
| **Python** | 3.9 or higher |
| **C Compiler** | GCC (via MinGW-w64 on Windows) — required to build `.exe` files |
| **Python Packages** | `tkinter` (built-in), `matplotlib`, `numpy` |
| **Fonts** | Trebuchet MS, Georgia, Cascadia Code (or Consolas as fallback) |

Install Python dependencies:
```bash
pip install matplotlib numpy
```

---

## 5. Installation & Build Guide

### Step 1 — Clone or download the project

Place all source files (`.py`, `.c`, `.h`) in a single project folder.

### Step 2 — Compile the C engines

The project includes a `Makefile`. From the project root in a terminal with GCC available:

```bash
make
```

This will compile and place `engine.exe` and `matrix_engine.exe` inside the `build/` directory.

If you do not have `make`, compile manually:

```bash
# Root Finding Engine
gcc src/engine_main.c src/roots.c src/tinyexpr.c -o build/engine.exe -lm

# Linear Systems Engine
gcc src/matrix_main.c src/linalg.c -o build/matrix_engine.exe -lm
```

### Step 3 — Verify the build

After compilation, confirm both executables exist:

```
build/
├── engine.exe          ✓
└── matrix_engine.exe   ✓
```

### Step 4 — Run the application

```bash
python main.py
```

---

## 6. License & Trial System

The application ships with a **24-hour free trial** managed through the Windows Registry.

### How it works

On first launch, `license_manager.py` creates a Registry key at:
```
HKEY_CURRENT_USER\Software\ArkoNumericalSuite
```

It stores two values: `InstallDate` (Unix timestamp) and `Status` (`"trial"` or `"unlocked"`).

| State | Behavior |
|---|---|
| **Trial active** | App runs normally; remaining hours shown in title bar |
| **Trial expired** | App hides main window and shows the Activation Gate |
| **Unlocked** | App runs permanently with no restrictions |

### Activating the software

When the trial expires, an activation dialog will appear. Enter the correct license key and click **ACTIVATE PERMANENT ACCESS**. The key is validated by comparing its SHA-256 hash against the value stored in `secret_key.py`.

```python
# secret_key.py
VALID_KEY_HASH = "18afb7f4d3a421ea9bf303a8a12c0430056e908643707a58a447a9ad920b014c"
```

To generate a new license key hash for distribution:
```python
import hashlib
key = "YOUR-LICENSE-KEY-HERE"
print(hashlib.sha256(key.encode()).hexdigest())
# Paste this output into secret_key.py
```

> **Note:** Closing the activation dialog without a valid key will quit the application entirely.

---

## 7. Running the Application

```bash
python main.py
```

The application opens at **1440 × 900** pixels. The main window contains three tabs managed by a `ttk.Notebook` with hidden tab headers (navigation is done via Dashboard buttons).

---

## 8. User Interface Guide

### 8.1 Dashboard (Home Tab)

The home screen is the central navigation hub.

```
┌────────────────────────────────────────────────────┐
│        ✦  C-Powered Computation Engine  ✦           │
│          Numerical Analysis Suite                   │
│  ─────────────────────────────────────────────────  │
│  ┌──────────────────┐    ┌──────────────────────┐  │
│  │  📈 Root Finding  │    │  🧮 Linear Systems   │  │
│  │     Engine        │    │      Engine          │  │
│  │  [ Launch  → ]    │    │  [ Launch  → ]       │  │
│  └──────────────────┘    └──────────────────────┘  │
│                                                     │
│   ℹ About Software           ☀ Light Mode [btn]    │
└────────────────────────────────────────────────────┘
```

**Controls on this screen:**

| Element | Action |
|---|---|
| **Launch Module →** (cyan card) | Opens the Root Finding Engine |
| **Launch Module →** (amber card) | Opens the Linear Systems Engine |
| **☀ Light Mode / 🌙 Dark Mode** (top right) | Toggles the full application theme |
| **ℹ About Software** | Opens the About dialog with version and developer info |

---

### 8.2 Root Finding Engine

This module finds roots (zeros) of an arbitrary single-variable equation f(x) = 0.

#### Input Configuration Panel (Left Column)

| Field | Description |
|---|---|
| **Method selector** | Choose a bracketing or open method via radio buttons |
| **Tolerance (ε)** | Convergence criterion — iterations stop when approximate error < ε. Entered as a percentage (e.g., `0.0001` means 0.0001%) |
| **Equation f(x)** | The equation to solve. See [Equation Syntax](#11-equation-syntax-reference) |
| **Point a / Point b** | Bracket endpoints for bracketing methods |
| **Initial Guess (x0)** | Starting point for Newton, Modified Newton |
| **x0 and δ** | Initial point and perturbation fraction for Modified Secant |
| **x0 and x1** | Two starting points for the Secant method |

#### Live Preview Panel (Right Column)

As you type in the equation field, a small preview graph updates automatically (with a 300ms debounce) showing the curve over x ∈ [−5, 5]. This helps visually confirm bracket placement before running the solver.

#### Running the Solver

Click **▶ CALCULATE** to run. The C engine is called in a background thread so the UI remains responsive. On completion, the view switches to the **Result Screen**.

#### Result Screen Layout

```
[← BACK]  [Current xr]  [Approx Error]  [True Error]  [Target Root]
┌────────────────────────────────────┬─────────────────────┐
│                                    │  Step  │  xr  │ εa  │
│     Interactive Graph              │   0    │ 1.52  │ 0%  │
│     (pan + zoom)                   │   1    │ 1.48  │ 2%  │
│                                    │  ...   │  ...  │ ... │
│     X: ---  |  Y: ---  (coords)    │                     │
└────────────────────────────────────┴─────────────────────┘
```

**Stat Cards (top bar):**

| Card | Meaning |
|---|---|
| **Current xr** | The root approximation at the selected step |
| **Approx Error (εa)** | Relative approximate error between consecutive iterations |
| **True Error (εt)** | Absolute error vs. the internally computed true root |
| **Target Root** | True root computed by Newton's method internally for reference |

**Graph interactions:**

| Action | How |
|---|---|
| Zoom in/out | Mouse scroll wheel |
| Pan | Click and drag |
| Read coordinates | Mouse hover — shown in the bottom-right label |

**Step Table (right panel):** Click any row to jump directly to that iteration. The graph updates to show the geometric construction for that step.

---

### 8.3 Linear Systems Engine

This module solves an N×N system of linear equations Ax = b.

#### Input Configuration Panel (Left Column)

| Control | Description |
|---|---|
| **Solver Method** | Select one of 4 algorithms via radio buttons |
| **Matrix Size (N × N)** | Use `+` / `−` buttons to set N between 2 and 5 |
| **Tolerance (ε)** | *(Gauss-Seidel only)* Convergence threshold |
| **Max Iterations** | *(Gauss-Seidel only)* Iteration cap (default: 100) |

#### Augmented Matrix Grid (Right Column)

An interactive `N × (N+1)` grid is displayed representing `[ A | b ]`. Each cell is a text entry box. The vertical dashed separator visually distinguishes matrix A from the constant vector b.

The grid is pre-populated with an identity matrix for A and `[1, 2, ..., N]` for b. Edit any cell to enter your system.

Click **▶ SOLVE SYSTEM** to run the solver.

#### Result Screen Layout

```
[← BACK]   Step 3: Eliminated Row 2 using Row 1
┌──────────────────────────────┬────────────────────────┐
│                              │  Step │ Row Operation   │
│   Animated Matrix Display    │   0   │ Initial State   │
│   (color-coded cells)        │   1   │ R2 → R2−2.00 R1 │
│                              │   2   │ R3 → R3−1.50 R1 │
│                              │  ...  │ ...             │
└──────────────────────────────┴────────────────────────┘
```

**Matrix cell color coding:**

| Color | Meaning |
|---|---|
| **Cyan** | Standard matrix entry |
| **Amber** | Diagonal (pivot) element |
| **Green** | Augmented column (b vector) |
| **Red highlight** | Cell that changed in this step |

**Step Table (right panel):** Each row shows the exact row operation performed (e.g., `R3 → R3 − 1.50 R1`). Click any row to jump to that state. For Gauss-Seidel, the table shows the full equation being evaluated at each sub-iteration.

---

## 9. Supported Algorithms — Root Finding

### Bracketing Methods
These require an interval `[a, b]` where `f(a)` and `f(b)` have opposite signs.

| Method | Key Idea | Convergence |
|---|---|---|
| **Bisection** | Repeatedly halves the interval. Always converges. | Linear (slow but guaranteed) |
| **False Position** (Regula Falsi) | Uses a linear interpolant to estimate the root. Faster than bisection on smooth functions. | Linear (superlinear in some cases) |
| **Brent's Method** | Combines bisection, secant, and inverse quadratic interpolation. Robust and fast. | Superlinear |

### Open Methods
These require only an initial guess or two starting points. They can diverge if the initial point is poorly chosen.

| Method | Key Idea | Convergence |
|---|---|---|
| **Newton-Raphson** | Uses f(x) and f′(x) to project to the x-axis. | Quadratic |
| **Modified Newton** | Uses second derivative to handle repeated roots. | Cubic |
| **Secant** | Approximates derivative using two points. No derivative needed. | Superlinear (~1.618) |
| **Modified Secant** | Uses a single point plus a small perturbation δ. | Superlinear |
| **Fixed-Point Iteration** | Rearranges f(x)=0 into x=g(x) and iterates directly. | Linear (if |g′|<1) |

---

## 10. Supported Algorithms — Linear Systems

| Method | Strategy | Notes |
|---|---|---|
| **Gaussian Elimination** | Forward elimination to upper-triangular form, then back-substitution. Uses partial pivoting. | Direct method. O(n³). |
| **Gauss-Jordan** | Full elimination to reduced row echelon form. No back-substitution needed. | Direct. Higher operation count than Gaussian. |
| **LU Decomposition** | Factors A = LU (with pivoting), then solves Ly=b and Ux=y. | Direct. Efficient for multiple right-hand sides. |
| **Gauss-Seidel** | Iterative method. Updates each variable using the most recent values of others. | Iterative. Requires diagonally dominant matrix for guaranteed convergence. |

All direct methods use **partial pivoting** (swapping rows to place the largest pivot on the diagonal) to improve numerical stability.

---

## 11. Equation Syntax Reference

The equation is parsed by the embedded **TinyExpr** C library. Use standard mathematical notation:

| Operation | Syntax | Example |
|---|---|---|
| Addition / Subtraction | `+`, `-` | `x^2 - 4` |
| Multiplication | `*` | `2*x + 1` |
| Division | `/` | `1/x` |
| Exponentiation | `^` | `x^3` |
| Square root | `sqrt(...)` | `sqrt(x)` |
| Trigonometric | `sin(x)`, `cos(x)`, `tan(x)` | `sin(x) - x/2` |
| Exponential | `e^x` or `exp(x)` | `e^x - 3` |
| Natural log | `log(x)` | `log(x) - 1` |
| Absolute value | `abs(x)` | `abs(x) - 2` |
| Constants | `pi`, `e` | `sin(pi*x)` |

**Examples:**

```
x^3 - 2*x - 5          → classic cubic root finding
e^(-x) - x             → exponential equation
sin(x) - x^2 + 1       → trigonometric equation
log(x) - cos(x)        → combined transcendental
x^4 - 3*x^2 + 2        → polynomial with multiple roots
```

> **Tip:** The live preview graph on the Root Finding input screen updates as you type, helping you visually identify good bracket values `[a, b]` before running the solver.

---

## 12. Keyboard Shortcuts & Controls

These shortcuts work when you are on a **Result Screen** (after clicking Calculate or Solve).

| Key | Action |
|---|---|
| `→` (Right Arrow) | Advance to the next iteration step |
| `←` (Left Arrow) | Go back to the previous iteration step |
| `Space` | Advance to the next iteration step |

**Mouse controls (Root Finding graph only):**

| Action | Input |
|---|---|
| Zoom in | Scroll up |
| Zoom out | Scroll down |
| Pan view | Click and drag |
| Read coordinates | Hover over graph |

---

## 13. Theming System

The application supports **Dark Mode** and **Light Mode**, toggled via the button in the top-right corner of the window. The entire UI — including all Matplotlib plots — is redrawn instantly when the theme changes.

The theme is managed by the `Theme` class in `config.py`. All colors are centralized in the `COLORS` object (a `Theme` instance) and referenced throughout every UI file as `COLORS.bg`, `COLORS.accent_cyan`, etc.

| Mode | Background | Primary Accent | Secondary Accent |
|---|---|---|---|
| **Dark** | `#07090f` (near-black) | Cyan `#00d4ff` | Amber `#f0a020` |
| **Light** | `#f0f4f8` (soft gray) | Sky Blue `#0284c7` | Deep Amber `#d97706` |

When theme is toggled, `main.py` calls `COLORS.toggle()` followed by a full UI rebuild via `_build_ui()`, which destroys and recreates all tab widgets in the new color palette.

---

## 14. Source File Reference

### Python Files

| File | Responsibility |
|---|---|
| `main.py` | App entry point; `NumericalSuite` Tk root window; tab routing; theme toggle |
| `config.py` | `Theme` class; `FONTS` dict; `safe_float()`; `resource_path()` for PyInstaller |
| `home_tab.py` | Dashboard layout; module cards; About dialog |
| `root_tab.py` | Root finding UI; subprocess call to `engine.exe`; result parsing; graph rendering |
| `matrix_tab.py` | Linear systems UI; subprocess call to `matrix_engine.exe`; step parsing; matrix renderer |
| `widgets.py` | `GlowButton`, `StatCard`, `PremiumEntry`, `SectionLabel`, `IconButton`, `draw_rounded_rect` |
| `license_manager.py` | Windows Registry read/write; trial timer; unlock validation |
| `license_gate.py` | Activation modal `Toplevel` window |
| `secret_key.py` | Single constant: the SHA-256 hash of the valid license key |

### C Files

| File | Responsibility |
|---|---|
| `engine_main.c` | Parses CLI args; initializes TinyExpr; calls `run_root_solver()` |
| `roots.c` | Implements all 8 root-finding algorithms; outputs `CURVE`, `STEP`, `TRUEROOT` lines |
| `matrix_main.c` | Parses CLI args; builds `Matrix`/`Vector`; routes to solver |
| `linalg.c` | Implements Gaussian, Gauss-Jordan, LU, Gauss-Seidel; outputs `MATRIX`, `ROW`, `SWAP`, `ELIMINATE`, etc. |
| `tinyexpr.c` | Third-party library; recursive descent math expression parser |

### C Headers

| File | Declares |
|---|---|
| `roots.h` | `init_math_parser`, `cleanup_math_parser`, `f`, `df`, `d2f`, `run_root_solver` |
| `linalg.h` | `Matrix`, `Vector` structs; all solver and utility function prototypes |
| `tinyexpr.h` | `te_expr`, `te_variable`, `te_compile`, `te_eval`, `te_free` |

---

## 15. Building for Distribution (PyInstaller)

The project includes a `main.spec` file for PyInstaller. To build a standalone `.exe`:

```bash
pip install pyinstaller
pyinstaller main.spec
```

The `resource_path()` function in `config.py` handles the `sys._MEIPASS` path used by PyInstaller to locate bundled resources (including the C `.exe` files and `app_icon.ico`) at runtime.

Make sure `engine.exe`, `matrix_engine.exe`, and `app_icon.ico` are listed in the `datas` section of `main.spec` before building:

```python
# Inside main.spec
datas=[
    ('build/engine.exe', 'build'),
    ('build/matrix_engine.exe', 'build'),
    ('app_icon.ico', '.'),
],
```

---

## 16. Troubleshooting

| Symptom | Likely Cause | Solution |
|---|---|---|
| `FileNotFoundError: engine.exe` | C engines not compiled | Run `make` or compile manually (see §5) |
| Graph area is blank after Calculate | Equation syntax error | Check equation format in §11; watch the preview graph |
| `ERROR,Invalid equation syntax` in solver | TinyExpr cannot parse the equation | Use `*` for multiplication; no implicit multiplication (`2x` → `2*x`) |
| App quits immediately on trial expiry | License gate closed without a key | Enter valid license key or reset Registry key manually |
| `winreg` import error | Running on non-Windows OS | `license_manager.py` uses Windows-only `winreg`. Port required for Linux/macOS |
| Fonts look wrong | Missing Cascadia Code font | Install [Cascadia Code](https://github.com/microsoft/cascadia-code) or update `FONTS` in `config.py` |
| Gauss-Seidel does not converge | Matrix not diagonally dominant | Rearrange equations so that the absolute value of each diagonal element exceeds the sum of the other entries in its row |
| `ERROR,Zero pivot encountered` | Singular or near-singular matrix | Check that the system has a unique solution; try reordering equations |

---

*Numerical Analysis Suite — Pro Edition · © Somajit Deb, Khulna University*
